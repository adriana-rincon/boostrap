let product=[];
let total=0

    function add (product, prince){
        console.log (product, price);
        products. push(product);
        total=total + price;
        document.getElementById("checkout").innerHTML= {total}
    }
    function pay(){
        console.log(product, price);
        window.alert(products.join(", /n"));
    }